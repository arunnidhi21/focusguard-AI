const canvas=document.getElementById('space'),ctx=canvas.getContext('2d');
let w,h,dpr,stars=[]; function resize(){dpr=Math.min(devicePixelRatio||1,2);w=innerWidth;h=innerHeight;canvas.width=w*dpr;canvas.height=h*dpr;canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);stars=Array.from({length:Math.min(130,Math.floor(w/7))},()=>({x:Math.random()*w,y:Math.random()*h,r:Math.random()*1.5+.25,v:Math.random()*.18+.04,p:Math.random()*6.28}))}resize();addEventListener('resize',resize);
let mx=0,my=0,tx=0,ty=0; addEventListener('pointermove',e=>{mx=e.clientX/w-.5;my=e.clientY/h-.5;tx=mx*18;ty=my*12});
function draw(t){ctx.clearRect(0,0,w,h);stars.forEach(s=>{s.y+=s.v;if(s.y>h)s.y=-2;let a=.3+.35*Math.sin(t*.001+s.p);ctx.globalAlpha=a;ctx.fillStyle='#8beef2';ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill()});ctx.globalAlpha=1;requestAnimationFrame(draw)}requestAnimationFrame(draw);
const scene=document.getElementById('scene'); function sceneMove(){scene.style.transform=`translate3d(${tx*.45}px,${ty*.25}px,0) rotateX(${-ty*.16}deg) rotateY(${tx*.16}deg)`}addEventListener('pointermove',sceneMove);
const tilt=document.querySelector('[data-tilt]');addEventListener('pointermove',e=>{if(!tilt)return;const r=tilt.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;tilt.querySelector('.tilt-inner').style.transform=`rotateX(${8-y*16}deg) rotateY(${-12+x*20}deg)`});tilt?.addEventListener('pointerleave',()=>tilt.querySelector('.tilt-inner').style.transform='rotateX(8deg) rotateY(-12deg)');
const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('show')),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));
